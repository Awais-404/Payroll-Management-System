#include <iostream>
#include <vector>
#include "Employee.h"
#include "Employee_Portal.h"
using namespace std;

main(){
    get_employee_data();
    get_base_pays();
    cout<<left;
    string s = "HELLO";
    s = s.substr(0,s.length()-1);
    cout<<s;
    return 0;
}